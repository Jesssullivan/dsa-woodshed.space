build
.svelte-kit
node_modules
static
*.md
# Authored content mirrored from the study packet repo — not our style domain.
src/content
pnpm-lock.yaml
# CI checks out the packet repo INSIDE the workspace (deploy-key content
# sync); it is a content source, never our style domain.
dsa-study-packet
