# PR / main gate: the same suite the repo runs locally (svelte-check, prettier
# + eslint, vitest, full build with the pagefind postbuild verifier, playwright
# overflow e2e). Content comes from the packet repo exactly as deploy-pages.yml
# gets it — the packet is public, no checkout credential needed.
name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gates:
    name: check / lint / unit / build / e2e
    runs-on: tinyland-docker
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7

      - name: Check out the study packet
        uses: actions/checkout@v7
        with:
          repository: Jesssullivan/dsa-study-packet
          path: dsa-study-packet

      # Self-hosted runner image ships no system node: setup-node must run
      # BEFORE pnpm/action-setup (whose self-installer needs node), and
      # `cache: pnpm` can't be used (it needs pnpm before pnpm exists).
      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      - uses: pnpm/action-setup@v6
        with:
          version: 10.13.1

      - run: pnpm install --frozen-lockfile

      - name: Sync content from the packet
        run: pnpm run sync-content
        env:
          WOODSHED_PACKET_PATH: ${{ github.workspace }}/dsa-study-packet

      - run: pnpm run check
      - run: pnpm run lint
      - run: pnpm run test:unit
      - run: pnpm run build

      - name: Install playwright chromium
        # The runner image pre-bakes Playwright's system libs — browser
        # binary download only, no runtime apt (--with-deps) needed.
        run: pnpm exec playwright install chromium
      - run: pnpm run test:e2e
